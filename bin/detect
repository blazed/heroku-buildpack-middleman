#!/usr/bin/env bash
# bin/detect <build-dir>

if [ -f $1/$APP_SUBDIR/config.rb ]; then
    grep "middleman" $1/$APP_SUBDIR/Gemfile.lock >/dev/null
    if [ $? -eq 0 ]; then
        echo "Middleman"
        exit 0
    fi
fi

exit 1
